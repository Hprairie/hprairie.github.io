source 'https://rubygems.org'

# For GitHub Pages compatibility
# github-pages includes jekyll and other dependencies
gem 'github-pages', group: :jekyll_plugins

# Required for Ruby 3.4.0+ (erb was moved to a separate gem)
gem 'erb'